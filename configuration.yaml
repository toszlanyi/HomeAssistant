
# Loads default set of integrations. Do not remove.
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

automation: !include_dir_merge_list automations/
script: !include scripts.yaml
scene: !include scenes.yaml

homeassistant:
  packages: !include_dir_named sensor_configs # (für separate YAML Dateien)

recorder:
  commit_interval: 60 # nur alle x Sekunden in Datenbank schreiben
  purge_keep_days: 10  # alte Daten löschen
#  include:
#    entity_globs:
#      - sensor.*
#    entities:
#      - sensor.
#  exclude:
#    entity_globs:
#      - sensor.*

# nur krasse Sachen loggen
#logger:
#  default: warning
#  logs:
    # Unterdrückt die API-Verbindungsfehler (172.30.32.2)
#    homeassistant.components.hassio: error
#    homeassistant.components.hassio.handler: error

    # Unterdrückt den Log-Spam des Supervisors selbst
#   # (die veralteten Felder in den Add-on Metadaten)
#    supervisor.resolution.checks.base: error
#    supervisor.resolution.check: error
#    supervisor.resolution.evaluate: error

    # Modbus-Einstellungen
#    homeassistant.components.modbus: warning
#    pymodbus.logging: warning
    
    # InfluxDB nur Errors zeigen
#    homeassistant.components.influxdb: error
    
    # DNS logger (Namen für Geräte finden)
#    homeassistant.components.dns: error
